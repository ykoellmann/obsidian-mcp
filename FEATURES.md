# Features

The supported MCP surface is documented in [README](README.md#tool-reference) and
the [interface contract](docs/mcp-surface-harmonization.md). This branch uses one
17-tool base catalogue with raw Markdown reads, safe literal edits, paginated
search/discovery, backlinks/tasks and attachments. There are no legacy aliases or
focused/full profiles. Optional format groups remain separately enabled.
